function calculateArea(rectLength, rectWidth) {
    return rectLength * rectWidth;
}
var rectLength = 5;
var rectWidth = 10;
var area = calculateArea(rectLength, rectWidth);
console.log("The area of the rectangle is: ".concat(area));
